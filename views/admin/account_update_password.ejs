<!doctype html>
<html lang="th">
<head>
  <title>เปลี่ยนรหัสผ่านบัญชี | <%=settings.company_name%></title>
  <%-include('includes/head')%>
</head>
<body>
  <%-include('includes/topbar')%>

  <!-- Desktop Sidebar -->
  <%-include('includes/desktop-sidebar')%>

  <!-- Mobile Sidebar -->
  <%-include('includes/mobile-sidebar')%>

  <div class="main-wrapper">

    <main class="container-fluid p-3 p-lg-4">
      <div id="alertWarpper"></div>
      <h2 class="fw-bold mb-3">เปลี่ยนรหัสผ่านบัญชี</h2>
      <form action="" method="post">
        <div class="table-responsive">
          <table class="table">
            <tbody>
              <tr>
                <td>รหัสผ่านปัจจุบัน</td>
                <td><input type="password" name="currentPassword" class="form-control"></td>
              </tr>
              <tr>
                <td>รหัสผ่านใหม่</td>
                <td><input type="password" name="newPassword" class="form-control"></td>
              </tr>
            </tbody>
          </table>
        </div>
        <button type="submit" class="btn btn-primary w-100"><i class="bi bi-unlock me-2"></i>เปลี่ยนรหัสผ่าน</button>
      </form>
    </main>
  </div>
  <%-include('includes/footer')%>
</body>
</html>
